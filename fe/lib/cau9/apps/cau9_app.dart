import 'package:flutter/material.dart';
import '../views/swipe_image_screen.dart';

class Cau9App extends StatelessWidget {
  const Cau9App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SwipeImageScreen();
  }
}
