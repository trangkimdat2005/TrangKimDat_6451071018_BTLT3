import 'package:flutter/material.dart';
import '../views/custom_slider_screen.dart';

class Cau7App extends StatelessWidget {
  const Cau7App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomSliderScreen();
  }
}
