import 'package:flutter/material.dart';
import '../views/tap_counter_screen.dart';

class Cau4App extends StatelessWidget {
  const Cau4App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TapCounterScreen();
  }
}
