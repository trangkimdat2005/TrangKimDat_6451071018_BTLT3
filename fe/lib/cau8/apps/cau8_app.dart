import 'package:flutter/material.dart';
import '../views/gesture_grid_screen.dart';

class Cau8App extends StatelessWidget {
  const Cau8App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GestureGridScreen();
  }
}
