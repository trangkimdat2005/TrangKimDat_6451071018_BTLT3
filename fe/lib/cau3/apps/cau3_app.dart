import 'package:flutter/material.dart';
import '../views/long_press_snackbar_screen.dart';

class Cau3App extends StatelessWidget {
  const Cau3App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LongPressSnackbarScreen();
  }
}
